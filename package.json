{
  "scripts": {
    "start": "netlify dev",
    "build": "npm run build.favicon && cmless build",
    "build.favicon": "icon-gen -i public/favicon.svg -o public -r --favicon name=favicon ico=16,24,32,64 sizes=192,512"
  },
  "dependencies": {
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "styled-components": "5.3.6"
  },
  "devDependencies": {
    "cmless": "^3.2.0",
    "icon-gen": "^2.0.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
